---
tags: [spec, tasks, templates]
created: "2026-06-24"
---

# Tasks - OPS-016-vault-hooks-doctor-check

> TDD order. One task = one focused commit. Tick as you go.

## Setup

- [x] Branch from main: `feat/dotf-doctor-vault-hooks` (descriptive; the spec id is the SSOT, not the branch)
- [x] `proposal.md` complete and acceptance criteria testable
- [x] No open questions left in `proposal.md` "Risks / open questions"

## Implementation

- [x] Add `CommandOutputDir` seam to `System` (`pre-commit install` resolves the repo from cwd) — `cli/internal/doctor/system.go`
- [x] Write `checkVaultHooks` mirroring `checkGuardHooks` (verify always, repair under `--fix`, idempotent) — `cli/internal/doctor/checks_vault_hooks.go`
- [x] Reuse the `VAULT_PATH` seam from `checkVault`; SKIP when no vault, loud FAIL when vault present but `pre-commit` missing
- [x] Detect installed hooks via the `generated by pre-commit` marker (rejects foreign/hand-rolled hooks)
- [x] Register `checkVaultHooks` in the `dotf doctor` sweep after `checkVault` — `cli/internal/doctor/doctor.go`
- [x] Table tests covering all six acceptance criteria — `cli/internal/doctor/checks_vault_hooks_test.go`

## Closing

- [x] Every acceptance criterion covered by ≥1 test
- [x] Every acceptance criterion has a matching entry in `features.json` with a non-vacuous verification command
- [x] Type checks pass (`go vet`)
- [x] Lint passes (CI `lint` green)
- [x] No unrelated changes in the diff (no scope creep)
- [x] `verification.md` filled in
- [x] PR opened referencing this spec folder (#553)

## Machine-readable features

This spec emits a sibling `features.json` following [[pattern-feature-list-as-primitive]]. Each acceptance criterion maps to ≥1 feature with `id`, `behavior`, `verification` (executable command), `state`, and `evidence`.

**Pass-state gating:** the agent CANNOT write `"state": "passing"` — only the harness, after running `verification` and capturing exit 0, may set that terminal state. Entries below stay `pending`; human-readable evidence lives in `verification.md`.
