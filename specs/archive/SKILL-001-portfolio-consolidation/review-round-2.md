---
spec: "SKILL-001-portfolio-consolidation"
verdict: "FAIL"
reviewed_sha: "c6e46216bff6dcea78068bbb6804b08df32efb25"
reviewer: "nan/deepseek-v4-flash"
date: "2026-09-25"
---

## Adversarial review

**Scope**: `SKILL-001-portfolio-consolidation` — `git diff 737c83c5567fd899c525fb0b13c59a4c34732b70...HEAD` (`737c83c..c6e4621`, 45 files, +546/−954). The range contains the implementation merge `3c2e393` (#1694, already on main) plus the three spec-recording commits; `737c83c` is an ancestor of `3c2e393` (verified with `git merge-base --is-ancestor`), so the range is the whole change and nothing unrelated.
**Sources**: `specs/SKILL-001-portfolio-consolidation/{proposal,tasks,verification,features.json}`, `harness/reviewer-pool.json` (reviewer drawn by the launcher), the vault at `$VAULT_PATH` (`00_meta/skills/`, `90_archive/skills/`, `00_meta/agents/definitions/`), `harness/manifest.json`, `scripts/compile-harness.sh`, `cli/internal/harness/*`.

### Spec and task alignment

Every task box in `tasks.md` is ticked; each was checked against the diff or the artifact it names, not the tick.

- **AC1** — verified. `00_meta/skills/` holds 31 skill directories; all eight retired names sit under `90_archive/skills/` with `status: archived` and a banner naming the successor section (`audit` → adversarial-review §Step 3; `verification-before-completion` → "Evidence before claims"/"Closing pass"; `writing-plans`/`executing-plans` → spec; `enrich-us` → new-ticket; the three no-persona skills say so instead of inventing a successor). f1 exits 0.
- **AC2** — verified, and the guard fires. `harness/skills/` has 31 records, no retired one; no `requires:` names a retired skill (all five `requires:` in the tree are inline flow lists, so f2's `^requires:` regex is adequate to the current shapes). Mutations prove the new guard is real, not decorative: adding `writing-plans` to the embedded `golang-engineering` trigger → `TestEverySkillTheRouterNamesHasARecord` fails naming the rule (`triggers_test.go:476`); adding `project-maturation` to `DefaultSkillDependencies[vault-doctor]` → the same test fails naming the map. Both reverted. Disk/embedded drift is separately caught by `TestTriggersEmbeddedMatchesDiskSSOT`.
- **AC3** — verified by measurement, not by the test's word. The resolving-rule count is **16 of 18 both before and after** (probe run against `737c83c` and against `c6e4621`): the two unresolved rules are `shell-standards` and `powershell-ascii-only`, which are pattern-only by design, so the floor of 16 is the baseline and not a quietly-accepted regression. The only routing change is `git-workflow` gaining `reviewer` (from `pr-review-triage` on that roster). The four domain triggers kept resolving to `builder` (base resolved them through the now-removed domain rosters, head through the added `test`/`systematic-debugging`), and the hook still names the domain skill first and prints the same persona set — verified with `dotf harness suggest --from-hook` on the Go, firmware, MCP and asyncio prompts, against both trees. Severities are as promised (`resolve-skills` prints the three slimmed rosters; warn counts 3/1/2).
- **AC4** — substantively verified by reading each fold against the archived original (`verification-before-completion`, `writing-plans`, `executing-plans`, `enrich-us`, `audit`): the Iron Law, the 5-step gate, the claims table, the closing-pass table and its three rules, the task-granularity rules, exact paths/commands, DRY/YAGNI, the execution batch rules and the "never start on main" rule all survive. Round 1's dispositions were applied, not just recorded (the letter-and-spirit clause is back; the done-item case is back in new-ticket). The evidence for the criterion is heading-level only — see F5.
- **AC5** — verified by running it. `go test ./...` ok; `GOOS=windows go vet ./...` and `go vet ./...` clean; `golangci-lint run ./...` 0 issues; `shellcheck --severity=error scripts/compile-harness.sh` clean; `compile-harness.sh --check` "no harness drift"; the 8,000-character doctrine test passes; the four-bats-file run f5 names is 127/127 green; the full suite is `1..1600`, 1599 ok, **1 failure** (`tests/shell-alias-collision.bats`, "the vendored oh-my-zsh git-plugin snapshot is still fresh against the real install"), which I reproduced identically on the base tree, so it is environmental and outside this change. `verification.md`'s count for that run is off by one (F2).
- **AC6** — the deployed state is genuinely clean, and I verified it independently of f6: no rendered copy of a retired skill carries the vault-source mark in any of the six `skills.deploy` targets, and no forced-roster or catalog line names one in `~/.claude/CLAUDE.md`, `~/.gemini/GEMINI.md`, `~/.copilot/copilot-instructions.md`, `~/.config/opencode/AGENTS.md` or `~/.pi/agent/AGENTS.md`. The recorded deploy is internally consistent: `3c2e393` already lacks the eight records, so a `--deploy` from main prunes the 48 copies (8 skills × 6 targets) the evidence reports, and "after merge" is satisfied. **The check that is supposed to certify this does not cover all the surfaces it claims to** — F1, and it is the reason this review fails.
- **Contract freshness** — recomputed the three contract digests exactly as `cli/internal/spec/contract_digest.go` normalises them (checkbox state folded, `features.json` `state`/`evidence` stripped, HTML escaping as `encoding/json` emits it): `proposal.md 200066ca…`, `tasks.md c1c2da34…`, `features.json c0ea422b…` — all three equal the launcher's `review-request.json`. Nothing in the contract set moved. No `[AGENT-DRAFT]`/`[AGENT-SUGGESTION]` tag remains in any spec file.

### Findings

| Severity | Reality | Area | Finding | Evidence | Test (named, or UNTESTED) | Fix location (code / tests / spec / vault) |
|----------|---------|------|---------|----------|---------------------------|---------------------------------------------|
| Major | REAL | verification coverage (AC6) | **f6 does not check the surfaces its own behavior string and AC6 name.** It hand-writes six skill paths and three instruction files instead of reading `harness/manifest.json`, so it is blind to two of the five pipeline-rendered instruction files that carry a **forced roster**: `agents.presence` declares `.config/opencode/AGENTS.md` and `.pi/agent/AGENTS.md`, both of which receive the `<!-- BEGIN HARNESS AGENT-PRESENCE … -->` region (markers at lines 279/290 of the live `~/.pi/agent/AGENTS.md`, and the same region in `~/.config/opencode/AGENTS.md`). **Reproduced:** with `HOME` seeded so those two files carry `MUST consume: [adversarial-review, verification-before-completion]` and the three checked files are clean, f6 **exits 0**; the control run with the same line in the checked `~/.claude/CLAUDE.md` exits 1. The gap is exactly the spec's own documented post-merge hazard: "a `--deploy` from a worktree whose branch predates it renders the eight records back into `$HOME`", which rewrites those two files and would leave f6 green. The hand-written list also silently under-covers any target added to the manifest later. | f6 seeded-HOME run (exit 0) vs control (exit 1); `harness/manifest.json` `skills.deploy` (6) + `agents.presence` (4); `AGENT-PRESENCE` markers in the deployed files; `scripts/compile-harness.sh` `deploy_skills`/`deploy_agent_presence` | **UNTESTED** — the new bats test `SKILL-001: deploy prunes a retired skill it rendered and keeps a foreign one of the same name` pins `deploy_prune`, a different mechanism; nothing relates f6's target list to the manifest | **spec** (`features.json`, the contract set → fix and re-review) **+ tests** (a bats test asserting f6's coverage equals `skills.deploy` ∪ `agents.presence`, so a new target cannot fall outside it) |
| Minor | REAL | evidence accuracy (AC5) | `verification.md` reports the full bats run as "1,599 run, 1 failure"; the run is `1..1600` (1599 ok + 1 not ok). The failure itself is correctly characterised as environmental. | My full-suite run: `1..1600`, 1 `not ok` = `tests/shell-alias-collision.bats` "the vendored oh-my-zsh git-plugin snapshot is still fresh against the real install"; reproduced identically on the base tree, and the file is untouched by the diff | named test: that bats case | verification.md (not contract set) |
| Minor | REAL | fold fidelity (AC4) | The folded evidence rule is **looser** than the rule it replaces and contradicts its own table. The archived original: "If you haven't run the verification command **in this message**, you cannot claim it passes." The fold: "If the command … has not run **in this session**, the claim is not yet made." Two lines below, the claims table still lists "A previous run" as not sufficient — a previous run in the same session is a previous run. | vault `90_archive/skills/verification-before-completion/SKILL.md` "The Iron Law" vs `harness/skills/adversarial-review/SKILL.md` §"Evidence before claims" (and its table row "Tests pass") | UNTESTED | vault (`00_meta/skills/adversarial-review/SKILL.md`) + refresh |
| Minor | REAL | dangling reference | A **live** vault pattern still names a retired skill as a consumer: `00_meta/patterns/pattern-architecture.md:13` — "Referenced by `architecture-session`, `enrich-us`, and the sensor-SDK reference audit". The `tasks.md` box "[AC2] Repoint every live reference: `requires:`, …, patterns, sibling skills, `skills/README.md`, `CURRENT-STATE.md`" is ticked. | `grep -rn 'enrich-us' $VAULT_PATH/00_meta` → that one hit; the vault commit `cee00c1f` touched only `adversarial-review`, `new-ticket` and an iris note | UNTESTED | vault (`00_meta/patterns/pattern-architecture.md`) |
| Minor | REAL | verification depth (AC4) | f4's proof that the folded rules are carried is six **heading** greps. Nothing pins the content: with `## Evidence before claims` kept and its whole body replaced by one placeholder line, f4 exits 0, the four `HARNESS-056` bats cases stay green, and `go test ./...` stays green. Round 1 already found one body-level loss inside these same folds, which is why the heading-only check is worth naming. | reproduced in a throwaway worktree at HEAD; reverted | UNTESTED | tests (extend f4 and/or `HARNESS-056` with a few load-bearing sentences, or ticket it) |
| Minor | REAL | knowledge handoff (DoD §2) | `verification.md`'s promotion-candidates list leaves the lesson candidate ("a forced roster is also a routing table (HARNESS-110), so slimming it silently unroutes prompts unless a guard counts resolving rules") unchecked, with no `docs/lessons/` entry and no ticket. Unchecked is silence, not a stated skip. | `git diff --stat … -- docs/lessons/` empty; the best candidate guard in the change (`TestRoleJoinDrift`) is pre-existing and its floor of 16 is now load-bearing | UNTESTED | repo `docs/lessons/` (or a ticket), and record the decision in verification.md |
| Minor | REAL | criterion/feature mismatch (AC5, declared round 1) | AC5 names "the full bats suite"; f5's behavior string narrows that to "the suites this change touches" and runs four files. The criterion is materially met (I ran the full suite), but the feature does not check what the criterion says, and it cannot: the suite has one environmental red. | f5 in `features.json` vs AC5 in `proposal.md`; my full-suite run | UNTESTED as a match (the files f5 names do pass, 127/127) | spec (`proposal.md` AC5 wording, contract set) or a recorded decision in verification.md |

Not a finding, recorded so the next round does not re-derive it: the "flat roster disarms the gate" risk (proposal, Risks) is caught only by f3's warn counts — I flattened `pr-review-triage` to a bare entry and the whole Go suite stayed green (warn 3→2), so f3 is the sole guard there. Also recorded: the reviewer roster grows 32% and gains a permanent `[gate] warn` for a skill only relevant to PR review; the proposal and `verification.md` both disclose it, so it is a declared trade, not a defect.

### Evaluator rubric

| Dimension | Grade (A-D) | Rationale (one line) |
|-----------|-------------|----------------------|
| Correctness        | B | The retirement, the rosters, the routing and the folds all hold up under measurement; AC6's evidence is narrower than the criterion it certifies. |
| Verification       | B | Commands are reproducible and I re-ran every one of them; one artifact (f6) passes on a state that violates the sentence it claims to test, and one count is wrong. |
| Scope              | B | Diff matches the proposal; the only side-changes (README/rc/runbook example swaps) are documented and were dispositioned in round 1. |
| Reliability        | B | Routing is measurably unchanged at 16/18 with the same two pattern-only rules; the one disarm path is guarded by a features command rather than a committed test. |
| Maintainability    | A | New Go guard is ~35 lines, single-purpose, heavily WHY-commented; `golangci-lint` 0 issues; folds read cleanly and cite their provenance. |
| Handoff-readiness  | B | Spec artifacts, decisions, deploy evidence and three follow-up tickets (HARNESS-147/153/154) are all in place; the lesson candidate is undecided (F6). |

### Verdict
FAIL

One **REAL Major** (F1), and it lives in the contract set: fixing it edits `features.json`, so the next round is a re-review by construction. Three things are genuinely in good shape and worth saying so plainly, because they constrain the next round: the vault/record halves agree (`--check` clean), the role join is measurably no worse than baseline, and the deployed machine state satisfies AC6 today — verified by grep, not by f6.

### Recommended next steps

1. **F1 (contract set, on FAIL: fix, then re-review).** Rewrite f6 to derive its targets from `harness/manifest.json` — `skills.deploy` for rendered copies, `agents.presence` (and the instruction/doctrine targets) for roster lines — instead of a hand-written list, and add a committed bats test that fails when the manifest declares a target f6 does not cover. Re-run f6 against a real `HOME` afterwards.
2. **F1, second half.** Because step 1 edits `features.json`, re-run `dotf spec review SKILL-001-portfolio-consolidation` before archiving; do not treat this verdict as reusable across that edit.
3. **Outside the contract set, apply freely (they do not stale this review's contract digests, but do stale its findings' text):** F3 (vault: restore "in this message", or make the session/message wording and the table agree) and F4 (vault: repoint `pattern-architecture.md`) — each followed by `compile-harness.sh --refresh` so the records stay the SSOT image of the vault.
4. **Record, in `verification.md`, the disposition of F2, F5, F6 and F7** — applied, ticketed, or declined with a reason — or carry F5/F6 into a follow-up ticket. `verification.md` is outside the staleness check precisely so this can be done without invalidating the verdict.
5. Do not archive yet. `dotf spec archive` will refuse without a fresh, passing `review.md`, which is the intended behaviour here rather than an obstacle.
