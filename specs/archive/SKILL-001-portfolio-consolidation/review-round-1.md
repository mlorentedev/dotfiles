---
spec: "SKILL-001-portfolio-consolidation"
verdict: "FAIL"
reviewed_sha: "e29c12895ce8a447b20582114bc44ed564e58b12"
reviewer: "nan/qwen3.8-flash"
date: "2026-09-25"
---
## Adversarial review

**Scope**: SKILL-001-portfolio-consolidation — `git diff 737c83c5567fd899c525fb0b13c59a4c34732b70...HEAD` (45 files, +491/−945; commits 3c2e393 merge of #1694, e29c128 spec-artifact record). Reviewer drawn from `harness/reviewer-pool.json` (`nan/qwen3.8-flash` is a pool member — verified against the file).

**Sources**: `specs/SKILL-001-portfolio-consolidation/{proposal,tasks,verification}.md` + `features.json`; the diff above; vault `90_archive/skills/` originals at `/home/manu/Projects/knowledge`; fresh runs of every feature command; two mutation edits (reverted); the deployed state of `$HOME` targets.

### Spec and task alignment

- **AC1 verified.** `00_meta/skills/` holds 31 skill directories (+ README/CURRENT-STATE); the eight retired skills exist under `90_archive/skills/` each with `status: archived` and a retirement banner naming the successor or the reason there is none. Repo `harness/skills/` mirrors: 31 records.
- **AC2 verified, and the guard is load-bearing.** No record for the eight; no `requires:` names one (repo and vault); both `triggers.json` copies are identically remapped and the embedded copy matches disk (`TestTriggersEmbeddedMatchesDiskSSOT`). Mutation-proved twice: (a) adding `enrich-us` to `task-and-ticket-tracking` in both copies made `TestEverySkillTheRouterNamesHasARecord` fail naming the trigger; (b) adding bare `- audit` to the reviewer roster made `TestEveryDeclaredSkillHasARecord` fail. Both reverted; tree clean.
- **AC3 verified by measurement.** `resolve-skills` prints exactly the three slimmed rosters; warn counts 3/1/2 match the frontmatter; a probe test (reverted) measured **16 of 18** rules resolving — the two non-resolving are `shell-standards` and `powershell-ascii-only`, `skills=[]` pattern-only rules, pre-existing. Severities preserved: the four surviving warn entries kept their level; the two flat builder entries stayed flat (`EnforceUnset` risk in the proposal is real and was avoided).
- **AC4 verified with the archived originals opened side by side.** The folded rules survived; three condensed rewrites dropped non-normative material (Minor #3).
- **AC5 verified fresh.** `go build`, `go vet`, `go test -count=1 ./...` all ok; `compile-harness.sh --check` exit 0; golangci-lint "0 issues"; shellcheck clean; full `bats tests/*.bats` in this session: 1,599 run, exactly 1 failure — "the vendored oh-my-zsh git-plugin snapshot is still fresh" (#1641 environmental), independently reproduced red in the plain main checkout `~/Projects/dotfiles` at 3c2e393. The 8,000-character doctrine guard ("committed records keep the capped agy surface under 8000 characters") passes. The five touched files: 192 ok / 0 fail.
- **AC6 — the repo-side mechanism is correct, but the criterion's verification command failed on fresh execution during this review.** See Blocker-free Major #1.
- Routing still works end-to-end: `harness suggest --from-hook` on "audit this code for security vulnerabilities" → `[persona] reviewer … adversarial-review`; on "enrich SDD-014" → planner set including `new-ticket`; on "write a plan for the migration" → planner including `spec`.
- No draft/suggestion tags remain in any contract file (the literal strings occur only in the gitignored `review-transcript.jsonl`, which is launcher-written and not a spec file). Normalised contract digests recomputed at review end match `review-request.json` byte-for-byte (proposal `db2e6f2f…`, tasks `2f0c9fa9…`, features `d06c7a03…`), so this verdict is fresh against the state it was launched on.

### Findings

| Severity | Reality | Area | Finding | Evidence | Test (named, or UNTESTED) | Fix location (code / tests / spec / vault) |
|----------|---------|------|---------|----------|---------------------------|---------------------------------------------|
| Major | REAL | AC6 verification (`features.json` f6, contract set) | f6 proves AC6 with bare `[ ! -e "$HOME/<target>/<name>" ]` on directories that are **multi-writer and multi-source**. Fresh execution at review start (~10:47, twice): **exit 1** — `$HOME/.claude/skills/audit/` and `$HOME/.pi/agent/skills/audit/` existed, each with a SKILL.md mtime 01:04. Provenance settled by size: our last rendered audit record (`git show 737c83c:harness/skills/audit/SKILL.md`) is 1,551 B and renders are subsets of the record; the observed copy was **1,892 B** — not ours. A foreign skill named `audit` shared the target dirs (cohabitants observed: `computer-use`, `find-skills`, `orca-cli`, `orchestration`, a dangling `frontend-design`). The foreign entry unloaded during the review (external write to `~/.claude` at 10:48:15) and the exact f6 string then **exit 0** (11:02). So AC6's substance held, but its named check went red for a reason AC6 does not cover, and can again on any third-party install; a false green is equally available if our copy ever re-appears under a pruned name. Second failure direction, on disk right now: **six worktrees** (`-wt-autocompact`, `-wt-handoff-legacy-block`, `-wt-handoff-thread-key`, `-wt-harness-hardening`, `-wt-memory-008-spec`, `-wt-personal-folder`) still carry `harness/skills/audit/SKILL.md`; a `--deploy` or session bootstrap from any of them reinstalls retired copies into the same shared targets. The proposal models the inverse hazard ("a `--refresh` from another branch drops the same eight records") — the reinstalling direction is unmodeled. | f6 exit 1 at ~10:47/10:50 (PRESENT lines, `ls -ld` with 1892-byte SKILL.md); `wc -c` on the base-commit record = 1551; f6 exit 0 at 11:02; `git worktree list` + per-worktree `-d harness/skills/audit`; findmnt shows plain ext4 (no overlay — the flip was a real external writer, not a filesystem view) | f6 (named — and it is the defect); no test distinguishes provenance → the fix itself is UNTESTED until a collision-named foreign skill is seeded in a FAKEHOME deploy test | **spec** (features.json f6 → provenance-aware: treat a retired name as present only when its SKILL.md carries `generated_from: 00_meta/skills/<name>`), **tests** (skills-pipeline: deploy prune survives a same-named foreign skill), **code/docs** (peer-rebase announcement for the six stale branches, tracked as a ticket) |
| Minor | REAL | AC4 fold fidelity | Condensed rewrites dropped non-normative material: `verification-before-completion`'s "Red Flags - STOP" list, the "Rationalization Prevention" excuse→reality table and the letter/spirit clause did not survive into `adversarial-review` ("Evidence before claims" has the Iron Law gate, the claims table and the Closing pass — all verbatim-equivalent); `new-ticket`'s "Enrich a thin item" dropped enrich-us's "backlog item already done ([x]): still allowed; enrichment becomes a post-hoc spec for audit" edge. `audit`'s standalone Output Format (HIGH/MEDIUM/LOW + "provide fixes for top 3") was deliberately superseded by the findings table — a design choice, recorded here, not a loss of a rule. | Archived originals vs records, read side by side (`90_archive/skills/verification-before-completion/SKILL.md` lines 62, 73; greps of `harness/skills/{adversarial-review,new-ticket}/SKILL.md`) | f4 — but it only greps section headings, so heading-present-but-content-short is invisible to the feature; content fidelity is UNTESTED | vault (skill bodies; records regenerate via `--refresh`; outside the contract set) |
| Minor | REAL | spec-vs-feature drift (AC5) | AC5 says "the full bats suite … pass"; the f5 command verifies `go test`, `--check` and **four** bats files. The full suite is honest evidence in `verification.md`, not in the feature command — and its one red is environmental (#1641), reproduced in this session on both trees, so "the full bats suite pass" is unattainable on this machine regardless of this change. | verification.md "Test status"; this review's full-suite run (1,599/1) and the same failure at `~/Projects/dotfiles` @ 3c2e393 | the environmental test itself, named, with its ticket | spec-adjacent: disposition in verification.md (contract files untouched); the #1641 fix is outside this spec |
| Minor | REAL | doc re-point semantics | README, `.bashrc`, `.zshrc`: the `> /audit src/auth.py` example became `> /test src/auth.py`. `test` scaffolds tests; it does not audit code. The trigger keyword route proves the correct successor: `suggest "audit this code for security vulnerabilities"` → `adversarial-review`. A user following the rc comment gets the wrong skill suggested. | git diff of the three files; `dotf harness suggest --from-hook` run this session | UNTESTED (rc comments are not asserted anywhere) | repo (three text edits) |
| Minor | SPECULATIVE | test fixture | `TestResolveDependencies` still names retired skills (`writing-plans`, `executing-plans`, `verification-before-completion`) inside its **synthetic** deps fixture. Functionally opaque ids; harmless. But it is the only place test code still spells retired names, and it reads like live dependencies. | `cli/internal/harness/triggers_test.go:261-281` | the test itself (green with any strings) | tests (cosmetic rename to clearly synthetic ids) |
| Minor | SPECULATIVE | vault link hygiene | Two vault files under `10_projects/` still link `00_meta/skills/dispatching-parallel-agents` (one session log — narrative history, fine; one research note — stale). Out of AC2's scope, which binds routing surfaces only. | greps of `$V/10_projects` | UNTESTED (repo guards do not scan `10_projects`) | vault (vault-doctor territory) |
| Question / assumption | — | cross-worktree deploy race | Do peers rebase now or at their own close? Until the six stale worktrees rebase onto ≥ 3c2e393, any of their `--deploy`/setup runs silently reinstalls the eight retired copies into shared `$HOME` targets. Nothing needs to happen for this spec, but the window is open today. | `git worktree list`; presence of `harness/skills/audit` in six branches at review time | n/a | coordination (announce/ticket; disposition in verification.md) |
| Question / assumption | — | schema choice the proposal defers to this review | "the schema-level alternative, a persona key for associated skills that the join reads but the gate does not, is left for the review of this PR to choose." Reviewer's choice: **keep the data-level fix** (the four domain rules gained a builder-roster skill). It is guarded on both sides now — `TestRoleJoinDrift`'s floor and the mutation-proved router guard — while the schema key is a public-contract change to the persona format, the gate and every record, and deserves its own spec and its own gate. File it as a follow-up idea; do not fold it here. | proposal.md "Risks"; tasks.md [AC3]; this review's mutation and probe runs | `TestRoleJoinDrift` (named, passes at 16/18) | vault (a future spec; a note in `00_meta/agents/ROSTER.md`'s related column already exists — this formalizes it) |

### Evaluator rubric

| Dimension | Grade (A-D) | Rationale (one line) |
|-----------|-------------|----------------------|
| Correctness        | C | AC1–AC5 verified fresh (including two mutation proofs and a measured 16/18); AC6's own named check exited 1 on fresh execution inside the review window and its passing state is not durable on the shared surface. |
| Verification       | B | Every claim re-runnable here was reproduced — build, vet, go test, --check, lint, shellcheck, full bats (1,599/1 environmental, reproduced red on main), all six feature commands; the f6 flip is honestly visible in machine mtimes rather than papered over. |
| Scope              | B | All 45 files map to proposal/tasks bullets; nothing unrelated. Two bullets mis-aimed (the `/test` example, the synthetic fixture names). |
| Reliability        | B | The router now fails closed on dangling names (mutation-proved); embedded-vs-disk triggers pinned; resolve-skills error semantics unchanged; no guard yet for cross-worktree deploy reinstalls. |
| Maintainability    | B | No new production logic; added test is small and its comment states the failure mode; records regenerate cleanly (`--check` green); folded sections readable. |
| Handoff-readiness  | B | Spec artifacts current and unusually honest; the one durable lesson ("a forced roster is also a routing table") is explicitly tracked as a promotion candidate with its tick reserved for the archive pass — deferred, not lost. |

### Verdict

**FAIL** — one **REAL Major** in the contract set: the verification command that AC6's `"state": "passing"` rests on (`features.json` f6) demonstrably does not prove what it claims, and went red on fresh execution during this review (exit 1 at ~10:47 and ~10:50; exit 0 at 11:02 after an unrelated external write). Under the skill's own rule, a REAL Major whose fix lands in `features.json` means **fix, then re-review** — the next round is the mechanism.

Everything else about this change survived the attempt to break it: the eight retirements, both trigger copies, the dependency map, the three rosters with every severity intact, the 16/18 join, the three folds compared against their archived originals, the TDD narrative (the new guards go red under mutation — I reintroduced `enrich-us` and `audit` myself and both named tests caught them), and the full suite plus the 8,000-char doctrine budget, run fresh here. The substance of AC6 — no dotfiles-rendered retired copy in any target — is true at review end. The defect is the thermometer, not the patient; but the archive gate reads the thermometer, so this round it decides.

### Recommended next steps

Minimum set to flip to PASS (round 2 re-review follows because item 1 moves a contract digest):

1. **features.json f6** — make the retired-name check provenance-aware: a retired skill counts as present only if its `SKILL.md` carries our render marker (`generated_from: 00_meta/skills/<name>`); foreign same-named skills in the shared targets then neither fake a red nor hide a green. Re-run the exact command fresh on the reference machine and record output and timestamp in f6's `evidence`.
2. **tests** (not blocking the verdict, but the right home for the regression): in `skills-pipeline.bats`' FAKEHOME deploy harness, seed a foreign same-named skill directory and assert `--deploy`'s prune survives it without deleting the foreign file, and f6-style verification still reports clean — that pins the distinction the bare `[ -e ]` cannot express.
3. **verification.md disposition** (outside the contract set, apply freely): record the review-window flip (10:47 red / 10:50 red / 11:02 green), the size proof that the observed `audit` copies were not ours, the six stale worktrees that can currently reinstate real copies via their `--deploy`, and the peer-rebase decision taken.
4. Answer the deferred schema question as this review's recommendation: keep the data-level fix; ticket the `related`-key persona-schema idea rather than widening this change.
5. Optional, non-gating: the `/test` → `/adversarial-review` example fix in README and both rc files; the synthetic fixture ids in `TestResolveDependencies`; the two dropped fold passages (Red Flags / Rationalization / already-done-item edge) into the vault skill bodies, followed by `--refresh`.

`dotf spec archive` is **not advisable** in the current state: this verdict is FAIL, so the gate should refuse until round 2 signs off the hardened f6. No commit, push or PR action was taken; the only file written is this `review.md`; the working tree carries only this artifact (contract digests verified unchanged from launch).
