---
spec: "HARNESS-145-doctrine-triggers-ledger-agy"
verdict: "PASS-WITH-GAPS"
reviewed_sha: "4e12d4542ca3b7d6bdd56fc05b6f562d9252bf52"
reviewer: "nan/deepseek-v4-flash"
date: "2026-09-25"
---

## Adversarial review

**Scope**: HARNESS-145-doctrine-triggers-ledger-agy — the whole change, `git diff 5abc54abf635831e0d1b1e33d29d645f8db976fb...HEAD` (base resolved by the launcher; 46 files, seven commits: S1 `ebc2e20`, S3 `3a8e52a`, S2 `1f833f4`, S4 `cd619b5`, release `e1c5754`, spec `4e12d45`).
**Sources**: `specs/HARNESS-145-doctrine-triggers-ledger-agy/{proposal,tasks,verification}.md`, `features.json`, `review-request.json`; live evidence on this machine (`~/.gemini/config/hooks.json`, `~/.gemini/settings.json`, `~/.local/state/dotfiles/gate/`, `~/.dotfiles/harness/triggers.json`, vault at `$VAULT_PATH`).

### Spec and task alignment

- **Every box in `tasks.md` is ticked, including the Closing block, and every `[x]` has diff evidence.** The one open box is "Independent review before archiving" — this artifact.
- **AC1**: reproduced. `compile-harness.sh --deploy` into a disposable `$HOME` from a detached worktree gives `.gemini/GEMINI.md` = 7,798 chars = 7,798 bytes, 0 bytes outside ASCII, against the test's 8,000 budget and agy's 12,000 cap; the live `~/.gemini/GEMINI.md` is the same 7,798/7,798/0. `bats tests/compile-harness.bats --filter 'committed records keep the capped agy surface under 8000 characters'` passes. The four named doctrine tests are load-bearing: dropping the section-sign rule from `fold_to_ascii` fails the ASCII test, and a catch-all `tr -d '\200-\377'` fails the "reported by its bytes" test.
- **AC2**: reproduced, and the real vault is checked, not only a fixture. `--refresh` against `$VAULT_PATH` is a byte-for-byte no-op on a clean checkout (0 files changed by `git status`) and prints `[refresh] triggers: every pattern named in harness/triggers.json exists in the vault`; all 18 patterns exist in the vault; `dotf doctor --verbose` prints `[ OK ] all 18 triggers name a pattern the vault has` against the deployed copy, and beware the deployed `harness/triggers.json` is byte-identical to the repo's. I confirmed the remap is not cosmetic: the eight targets are substantive nearest matches (`pattern-git-workflow` §12 is the IaC policy, `pattern-container-workflow` §5 is Production Kubernetes, `pattern-matlab-embedded` opens with "Evidence First" and a hardware/firmware debugging protocol), and the two `triggers.json` copies are held identical by `TestTriggersEmbeddedMatchesDiskSSOT`. The guard is load-bearing: `|| exit 1` → `|| true` fails the bats test, and moving the check after the first record write fails its `writes nothing` assertion. See findings 1 and 2 for the input classes it does not cover.
- **AC3**: reproduced. `TestSessionsWithNoIDShareNoLedger` fails (both subtests) when the empty-scope guards are reverted across `StatePath`, `DispatchPath` and the gate's unscoped branch — I ran that revert. `TestDecisionPathNeverFallsBackToTheDigestOfNothing`, `TestTheReservedJournalNameIsNotASession` and `TestConsumptionScopeNeedsASession` each fail when their guard is removed. The two-place redundancy (path builders plus the gate's early allow) means reverting `StatePath` alone leaves the tests green; that is defence in depth, not a test gap.
- **AC4**: reproduced. `TestAgyAnswersInItsOwnProtocol` fails both when `ask` is changed to `allow` and when the `agy` payload case is removed; `TestLoadBindTargetsRefusesAFormatInTheWrongKey` fails when the wrong-key refusal is neutralised. On this machine `~/.gemini/config/hooks.json` holds our `PreToolUse` named hook with `matcher: "*"` and `timeout: 5` beside Orca's untouched `orca-status`; `~/.gemini/settings.json` holds only Orca's four events (the `BeforeTool` gate is retired); and `dotf harness bind --dry-run` reports `ok claude` and `ok agy: hooks already current` with no pending retirement.
- **AC5**: the record exists and matches the shape claimed — `grep -h '"harness":"agy"'` over the gate state dir yields `"session":"8917b453-b620-4c9d-9d0e-ac1c21991bd9"`, `"tool":"view_file"`, `"outcome":"no-role"`, in `8917b453-b620-4c9d-9d0e-ac1c21991bd9-43d11dee.decisions.jsonl`. The feature's command exits 0 here, and I rebuilt its four negative controls (synthetic session, no session, `payload-unrecognised`, another harness): all four exit 1, as `features.json` claims. See finding 6 for what that command can and cannot prove.
- **Test suites**: `go build`, `go vet` (linux and windows configurations covered by the repo's own jobs), `go test -count=1 ./...` (25 packages, all ok), `golangci-lint run ./...` (0 issues), `shellcheck --severity=error scripts/compile-harness.sh`, `compile-harness.sh --check`, `check-doc-paths.sh`, `check-lessons.sh`: all pass. `bats tests/*.bats` serial: **1599 run, 1 failure** — `the vendored oh-my-zsh git-plugin snapshot is still fresh against the real install` (`tests/shell-alias-collision.bats`), which is the environmental #1641 snapshot and touches nothing in this diff.
- **Scope note**: the measured range includes two changes this spec does not own — `ee6d1e7` (#1684, the `handoff`/`pr-review-triage` skill records regenerated from the vault) and release `e1c5754` (0.58.0, whose changelog also carries the unrelated secrets-plane feature #1673). Both are adjacent-main noise between the resolved base and this work, not creep by this author; they are also exactly why the base must come from the launcher rather than `main`.
- **Knowledge placement**: lessons 292 and 293 are written and indexed, the vault keeps the pre-diet wording in `pattern-change-lifecycle` §PR Stewardship (original wording), `pattern-git-workflow` §10b and `pattern-secrets-security` §8b so nothing was lost, ADR-027 carries the amendment and ADR-010 the hooks row. Lesson 291's absence from the index is another session's allocation (`dotfiles-wt-personal-folder`, `b1d8ea6`), not a gap.

### Findings

| Severity | Reality | Area | Finding | Evidence | Test (named, or UNTESTED) | Fix location (code / tests / spec / vault) |
|----------|---------|------|---------|----------|---------------------------|---------------------------------------------|
| Minor | REAL | triggers guard (shell) | `--refresh` announces `every pattern named ... exists in the vault` when it read nothing: `jq`'s parse failure is swallowed by the process substitution (`while ... done < <(jq ...)`), so a malformed document iterates zero times and reports green; `{"triggers":[]}` does the same honestly. The AC2 guard therefore cannot tell "every target resolved" from "no target was examined" | Reproduced on a detached worktree at HEAD: `printf '{ this is not json' > harness/triggers.json; VAULT_PATH=... bash scripts/compile-harness.sh --refresh` prints `jq: parse error: Invalid literal at line 1, column 7`, then the positive `[refresh] triggers: ...` line, exit 0. `{"version":1,"triggers":[]}` prints the same line, exit 0 | UNTESTED — `triggers: --refresh fails naming the trigger and pattern, and writes nothing` uses a well-formed document with one dangling target. Backstop: `TestRealTriggersFileValid` (Go, same bytes) rejects an empty array and an empty `Pattern` | code + tests (`scripts/compile-harness.sh`: treat a zero-result read or a `jq` failure as a refusal, not a pass; the message should name the input it could not read) |
| Minor | THEORETICAL | doctor trigger check | `checkTriggerTargets` reports `[ OK ] all 0 triggers name a pattern the vault has` for a deployed document that names no pattern — a PASS with nothing checked, where the sibling "no triggers file" and "no vault" paths correctly SKIP. It is the vacuous-pass shape lesson 287 names and lesson 292 repeats | Probe against the check itself (three inputs — zero triggers, every rule with `pattern: ""`, `pattern` key absent — each `failures=0` followed by `[ OK ] all 0 triggers name a pattern the vault has`) | UNTESTED — no case asserts what a zero-named document must report; `TestCheckTriggerTargets_EveryPatternPresentPasses` covers only the 2-of-2 case | code + tests (`cli/internal/doctor/checks_trigger_targets.go`: `named == 0` → `rep.Skip("nothing to check")`; add the case) |
| Minor | THEORETICAL | gate state | `_unscoped` is reserved as a session id, `_unparsed` is not, though it plays the same journal role. A payload claiming `session_id: "_unparsed"` is treated as a real session and its decision journal is the payload-not-recognised journal, so one scope's history contains the other's. No gating state is shared (`StatePath` is a distinct file), so the cost is journal fidelity, not a bypass | Probe: `DecisionPath(dir, UnparsedScope)` and `DecisionPath(dir, "_unparsed")` are the same path (`.../gate/_unparsed-f90eab5c.decisions.jsonl`), while `ToolCall{SessionID: "_unparsed"}.ConsumptionScope()` returns `_unparsed`, i.e. it is honoured as a session | UNTESTED — `TestTheReservedJournalNameIsNotASession` exercises `_unscoped` only | code + tests (extend the reserved-name read to both constants), or one line in `UnscopedScope`'s comment saying why only one of the two is claimed |
| Minor | THEORETICAL | file permissions | The mode preservation added to `writeSettingsAtomically` applies to every bind target, not only the file shared with Orca. The previous writer chmod'ed 0600 unconditionally on each bind, so `~/.claude/settings.json` used to be tightened when written; now an existing 0664 is kept. Not a new exposure — the file was already 0664 — but a privacy-directional behaviour change outside the stated rationale, and nothing records the decision | Base code `git show 5abc54a:cli/internal/cmd/harness_bind.go` chmods `0o600` unconditionally; HEAD stats the target first. Live `~/.claude/settings.json` is `-rw-rw-r--` (mtime 02:42, i.e. written by the operational tail's bind); its `env` holds two feature flags and no secret | UNTESTED — the only mode assertion is the agy 0664 case (`harness_bind_test.go`, `a file shared with another tool keeps its mode`); no test pins the claude path | code (preserve the mode only for a document we share, tighten one we own) or a declared decision in `verification.md`; test either way |
| Minor | REAL | verification precision | Two claims in `verification.md` are wider than their evidence: (a) "Orca's `orca-status` hook byte-identical" — the write re-renders the whole document through `json.MarshalIndent` with Go-sorted keys, so Orca's entries change bytes while their JSON value is unchanged, and the test asserts value equality; (b) the bats totals for S3 and S4 (1,597) describe a branch based on S3 — the merged tree holds 1,599 `@test`, which is what a run on HEAD reports | (a) Live `~/.gemini/config/hooks.json` orders Orca handlers `command, timeout, type` while `hooks.json.bak` (2026-08-22) orders them `type, command, timeout`; the test marshals `doc["orca-status"]` before and after, i.e. compares values. (b) `git log` puts S2 (`1f833f4`) between S3 and S4, so S4's base carried S2's two tests; `grep -c '^@test' tests/*.bats` = 1,599 at HEAD, 1,593 at the base, S1 +4 and S2 +2 | UNTESTED — no test asserts file-byte identity, and no artifact re-measures the totals after S2 merged | spec (`verification.md` wording and counts only; excluded from the staleness check, so this can be corrected under this verdict) |
| Question / assumption | REAL | AC5 evidence | f5's verification command is a property of one machine, not of the change: it greps `~/.local/state/dotfiles/gate/*.decisions.jsonl`, so it can only ever exit 1 anywhere else, and once green it cannot go red. The behaviour it names is pinned in-process by AC4's tests, but the AC5 artifact itself cannot be re-verified by a second party — the previous verifier, as `verification.md` records, would have passed on synthetic records | The command exits 0 here (record for conversation `8917b453-b620-4c9d-9d0e-ac1c21991bd9`), and 1 on each of the four negative controls I rebuilt; on a clean checkout of this commit with no gate state dir it also exits 1 | `TestAgyAnswersInItsOwnProtocol` covers the record shape in-process; the live command is UNTESTED for reproducibility | tests (a fixture-driven assertion that the gate's own output carries the record shape, if AC5 is to be machine-checkable) or spec (`verification.md`: declare f5 a human-run operational check, as the AC5 wording already half-does) |

### Evaluator rubric

| Dimension | Grade (A-D) | Rationale (one line) |
|-----------|-------------|----------------------|
| Correctness        | B | Every acceptance criterion is met and I re-derived each one, including the negative paths (unreserved name, empty scope, unparseable payload, wrong-key manifest, unreadable shared file, dry run, idempotence); the two gaps are in the guards' own vacuous-input handling, not in the shipped behaviour. |
| Verification       | B | f1–f4 are exactly reproducible and I re-ran all of them, mutation-confirming that each named test fails when its fix is reverted, plus `go test`, the full `bats` suite, the linters and a full `--refresh`/`--deploy`; the deductions are AC5's machine-bound command and two overstated claims in `verification.md`. |
| Scope              | B | The four slices do what the proposal says and nothing else; the range also carries two adjacent-main changes this spec does not own (#1684, the 0.58.0 release), and one in-scope behaviour change was not announced (the write-mode preservation). |
| Reliability        | B | Error paths are handled deliberately — an unparseable settings file is refused rather than overwritten, the new hook home is written before the old entry is retired, an empty scope is a quiet no-op, a re-run is a byte-identical no-op — with the fail-open cases journaled under their own outcome; the retire is skipped when the harness binary is absent, which leaves a migrated machine's stale entry if `agy` is uninstalled. |
| Maintainability    | C | The new helpers are small and unusually well-argued (`MergeNamedHooks` CC 13, `RetireHooks` 11, `writeAgyVerdict` 3), but the changed decision closure grew from **CC 14 to 20** (`gocyclo`, measured on the base and on HEAD), crossing the >15 line this rubric sets and moving further from the repo's own stated `< 10` rule, which no linter enforces. |
| Handoff-readiness  | A | Spec artifacts are complete and consistent, lessons 292/293 are written and indexed, the vault keeps the pre-diet wording, ADR-027 and ADR-010 are amended, and the only open task box is this review. |

### Verdict

PASS WITH GAPS

Highest severity on the findings axis is Minor and no Major is REAL, so severity alone does not block; the rubric carries one **C** (Maintainability, measured, no D), which sets the floor at PASS WITH GAPS by the aggregation rule. `dotf spec archive` is **advisable** once the recommendations below are dispositioned — nothing here asks for a contract-set edit, so this verdict stays valid for the content it was written against (`proposal.md`, `tasks.md` and `features.json` digests match the launcher's record; I verified that with `ContractDigests`).

### Recommended next steps

- **Do not edit the contract set** (`proposal.md`, `tasks.md`, `features.json`). This verdict is fresh against those three digests; an edit invalidates it and costs another review round. Everything below lands in the non-contract set.
- **Disposition findings 1 and 2 in `verification.md`** — applied or ticketed with a reason. Both are one-branch fixes in a guard this change exists to make trustworthy: make a zero-result or unreadable `triggers.json` a refusal in `scripts/compile-harness.sh`, and make `checkTriggerTargets` SKIP on `named == 0` instead of PASSing. Add the missing case to each guard's tests.
- **Ticket or fix the CC 20 closure** (rubric C). Extract the unscoped/skill branch handling out of the `RunE` closure in `cli/internal/cmd/harness_gate.go` into named helpers; `gocyclo` on the file is the acceptance check, and no CI job enforces this today, so it needs a test or a check to stay fixed.
- **Decide and record the file-mode behaviour** (finding 4): either tighten files the bind owns and preserve the mode only for shared documents, or say in one line that a shared file is not ours to re-permission. Either is fine; silence is what a later reader cannot audit.
- **Correct `verification.md`** (finding 5): say "value-identical" for Orca's hook, and re-measure the bats totals on the merged tree (1,599 run, 1 failure — the #1641 oh-my-zsh snapshot, red on plain `main` here too).
- **Note AC5's standing** (finding 6): the record exists today and the four negative controls behave as claimed, but the check is machine-bound. If AC5 is to be re-verifiable, its shape belongs in a test; if not, say so where a future archivist reads it.
- Not re-proposed here: the agy parser remains documentation-derived, as the proposal discloses, and AC5's live record is its only confirmation — that is a declared risk with its mitigation named, not a finding.
