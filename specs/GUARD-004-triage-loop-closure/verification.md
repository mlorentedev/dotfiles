---
tags: [spec, verification, templates]
created: "2026-08-20"
---

# Verification — GUARD-004-triage-loop-closure

Every command below was run in this session, in the worktree at
`/home/manu/Projects/dotfiles-wt-loop`, against `origin/main` at `ecf712b`.

## Automated

| Check | Command | Result |
|---|---|---|
| Go build | `go build ./...` | pass |
| Go vet | `go vet ./...` | pass |
| Windows vet | `GOOS=windows go vet ./...` | pass |
| Go tests | `go test ./...` | pass, 0 failures |
| Lint (pinned 2.12.2) | `golangci-lint run` | **0 issues** |
| Shell/bats | `~/.local/bin/bats tests/*.bats` | 0 failures |

The linter was confirmed to match `versions.conf`'s pin before it was trusted —
a local binary on a different major reports "0 issues" on code CI rejects
(BUG-071).

## Per acceptance criterion

**AC1/AC2 — the skill writes the marker, quoted from the registry.**
`grep -c "## Review triage"` returns 2 in the vault SSOT and 2 in the regenerated
record; it returned 0 in both before the change. The heading is introduced as a
contract naming `harness/review-attestation.json` as its single declaration.

**AC3 — both assembly paths.** `TestBriefSkipsTriageWhenProbeIsNil` proves the
agnostic `Brief` renders the section when a probe is wired, and
`TestClaudeContextCarriesTriageSection` proves the Claude adapter does. Observed
live on the agnostic path, which is what opencode, agy and copilot consume:

```
$ SESSION_BRIEF_CWD=... dotf mem session-start --format stdout
[pr-triage] awaiting a disposition: #1097, #1085
  Dispose with /pr-review-triage. Nothing counts as triaged until its
  table lands on the PR under the registry's triage marker.
```

**AC4 — no registry, no section.** Run against `~/Projects/knowledge`, which
carries no reviewer registry: zero `pr-triage` lines. Not an error, not an empty
queue — absent.

**AC5 — present but unanswerable is loud.** A temporary git repo carrying only
the registry, run with `gh` off `PATH`:

```
[pr-triage] queue could not be computed: gh pr list: exit status 1
  This is not an empty queue. Run `dotf pr triage-queue` to see why.
```

This is the criterion the whole design turns on, and it was observed failing
first: `TestTriageQueueSection/"a failure is reported, never rendered as empty"`
asserts the message denies the empty reading in words, not merely by being
non-empty.

**AC6 — bounded.** `prtriage.Fetch` takes a `context.Context` and uses
`exec.CommandContext`; the session-start caller passes a 5s deadline.

**AC7 — the refactor is behaviour-preserving.** `dotf pr triage-queue` was run
before and after the extraction into `prtriage.Fetch` and produced the same
listing (#1097, #1085) with the same wording.

## Not verified here

The deployed skill under `~/.claude/skills/` is regenerated by
`compile-harness.sh --deploy`, which this PR does not run — the record in
`harness/` is what it deploys from, and that is updated. Deployment is verified
after merge by re-running setup, per the two-tier rule.
