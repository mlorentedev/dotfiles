{
  "mcpServers": {
    "hive-vault": {
      "command": "uvx",
      "args": [
        "hive-vault"
      ],
      "env": {
        "VAULT_PATH": "${VAULT_PATH}"
      },
      "timeout": 30000,
      "trust": false
    }
  }
}
