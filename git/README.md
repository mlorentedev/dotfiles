# Git Module

Git configuration with sensible defaults.

## Features

- Pre-configured user settings
- Useful aliases
- Better diffs and merges
- Color output

## Installation

Using Stow:
```bash
stow git
```

## Customization

Edit `~/.gitconfig` after installation or add `~/.gitconfig.local` for machine-specific settings.
