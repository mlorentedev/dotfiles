# CLAUDE.md

> **Read `AGENTS.md` first** — it is the canonical source of truth for how AI
> coding agents work in this repo (workflow, conventions, Spec-Driven
> Development). This file holds only Claude Code-specific notes layered on top.
>
> If a rule here ever conflicts with `AGENTS.md`, **`AGENTS.md` wins** for
> behaviour. Keep this file a thin pointer — put durable rules in `AGENTS.md` so
> every agent (Claude, Copilot, Cursor, Codex) sees them.

## Claude Code notes

<!-- Add Claude-specific tooling notes here (skills, MCP servers, hooks). -->
